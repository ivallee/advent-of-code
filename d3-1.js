function spiral(n) {

  let x = 0;
  let y = 0;
  let direction = 'EAST';
  let square = Math.pow(1, 1);

  const cycles = Math.floor((Math.sqrt(n + 1) - 1) / 2) + 1;

console.log('Spiral Cycles :', cycles);

}

spiral(400);

// 